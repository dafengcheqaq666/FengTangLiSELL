import { requireAdmin } from "@/lib/auth";
import { prisma } from "@/lib/prisma";
import { formatMoney } from "@/lib/money";
import { refundOrder, shipOrder, updateVariant, updateZone } from "./actions";

const statusLabel: Record<string, string> = { PENDING_PAYMENT: "待支付", PAID: "已支付", FULFILLING: "待发货", SHIPPED: "已发货", COMPLETED: "已完成", CANCELED: "已取消", REFUNDING: "退款中", REFUNDED: "已退款" };

export const dynamic = "force-dynamic";

export default async function AdminPage() {
  const admin = await requireAdmin();
  const [variants, orders, zones] = await Promise.all([
    prisma.productVariant.findMany({ include: { product: true }, orderBy: { sortOrder: "asc" } }),
    prisma.order.findMany({ include: { shipment: true }, orderBy: { createdAt: "desc" }, take: 50 }),
    prisma.shippingZone.findMany({ orderBy: { province: "asc" } }),
  ]);
  return <div className="admin-shell"><header className="admin-bar"><div className="container"><strong>山野蜜境 · 管理后台</strong><div>{admin.email}　<form action="/api/admin/logout" method="post" style={{ display: "inline" }}><button className="small-button">退出</button></form></div></div></header><main className="page-shell"><div className="container"><div className="admin-grid"><section className="card"><h2>商品与库存</h2><div className="table-wrap"><table className="data-table"><thead><tr><th>规格</th><th>价格</th><th>库存</th><th>预占</th><th /></tr></thead><tbody>{variants.map((variant) => <tr key={variant.id}><td>{variant.product.name}<br />{variant.name}</td><td colSpan={4}><form action={updateVariant} className="inline-form"><input type="hidden" name="id" value={variant.id} /><input name="price" type="number" min="0.01" step="0.01" defaultValue={(variant.priceFen / 100).toFixed(2)} style={{ width: 90 }} /><input name="stock" type="number" min="0" defaultValue={variant.stockOnHand} style={{ width: 75 }} /><span>预占 {variant.stockReserved}</span><button className="small-button">保存</button></form></td></tr>)}</tbody></table></div></section><section className="card"><h2>配送区域</h2><div className="table-wrap" style={{ maxHeight: 360 }}><table className="data-table"><thead><tr><th>省份</th><th>启用 / 运费</th></tr></thead><tbody>{zones.map((zone) => <tr key={zone.id}><td>{zone.province}</td><td><form action={updateZone} className="inline-form"><input type="hidden" name="id" value={zone.id} /><input type="checkbox" name="enabled" defaultChecked={zone.enabled} /><input type="number" name="fee" min="0" step="0.01" defaultValue={(zone.feeFen / 100).toFixed(2)} style={{ width: 75 }} /><button className="small-button">保存</button></form></td></tr>)}</tbody></table></div></section></div><section className="card" style={{ marginTop: 20 }}><h2>最近订单</h2><div className="table-wrap"><table className="data-table"><thead><tr><th>订单</th><th>收货人</th><th>金额</th><th>状态</th><th>履约 / 售后</th></tr></thead><tbody>{orders.map((order) => <tr key={order.id}><td>{order.orderNo}<br /><span className="muted">{order.createdAt.toLocaleString("zh-CN")}</span></td><td>{order.recipientName}<br />{order.province}{order.city}</td><td>{formatMoney(order.totalFen)}</td><td><span className="status-pill">{statusLabel[order.status]}</span></td><td>{["PAID","FULFILLING"].includes(order.status) && <div><form action={shipOrder} className="inline-form"><input type="hidden" name="orderId" value={order.id} /><input name="carrier" placeholder="承运商" size={7} /><input name="trackingNo" placeholder="运单号" size={14} /><button className="small-button">发货</button></form><form action={refundOrder} className="inline-form" style={{ marginTop: 6 }}><input type="hidden" name="orderId" value={order.id} /><input name="reason" defaultValue="管理员整单退款" /><button className="small-button danger-button">退款</button></form></div>}{order.shipment && <span>{order.shipment.carrier} · {order.shipment.trackingNo}</span>}</td></tr>)}</tbody></table></div></section></div></main></div>;
}
